import Foundation

struct TimeIntervalFormatterUtil {
    static func formatToMinutesAndSeconds(_ timeInterval: TimeInterval) -> String {
        let minutes = Int(timeInterval) / 60
        let seconds = Int(timeInterval) % 60
        return String(format: "%02d:%02d", minutes, seconds)
    }
}
